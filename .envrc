watch_file pixi.toml
use pixi

# Local dev only: route git hooks to the committed .githooks/ dir. Skip in CI,
# which should check code, not auto-fix it. (idempotent)
if [ -z "${CI:-}" ]; then
  git config core.hooksPath .githooks
fi
