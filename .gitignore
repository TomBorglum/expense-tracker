# Pixi environment
.pixi/

# Python bytecode
__pycache__/
*.py[cod]

# Frontend dependencies (restored with `pixi run web-install`).
# Note: src/expense_tracker/static/ is the vite build output and IS committed, so
# the wheel is self-contained and the prod environment needs no Node.
node_modules/

# Test/coverage artifacts
.pytest_cache/
.coverage
coverage.xml
