[pytest]
testpaths = tests
addopts = -ra
# tests/ is not a package, so pytest prepends tests/ rather than the repo root to
# sys.path. This makes the expense_tracker package importable from the tests.
pythonpath = .
