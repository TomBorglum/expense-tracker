[workspace]
name = "expense-tracker"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = "==3.14.6"
ruff = "==0.15.21"
mypy = "==2.2.0"
pytest = "==9.1.1"

[tasks]
lint = "ruff check ."
format = "ruff format ."
typecheck = "mypy"
test = "pytest"
